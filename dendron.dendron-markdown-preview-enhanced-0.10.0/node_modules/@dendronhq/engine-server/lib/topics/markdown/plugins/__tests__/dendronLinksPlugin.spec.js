"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const unified_1 = __importDefault(require("unified"));
const dendronLinksPlugin_1 = require("../dendronLinksPlugin");
const remark_parse_1 = __importDefault(require("remark-parse"));
function getProcessor() {
    return unified_1.default().use(remark_parse_1.default, { gfm: true }).use(dendronLinksPlugin_1.dendronLinksPlugin);
}
describe("basic", () => {
    test("init", () => {
        const resp = getProcessor().parse(`[["foo.md"]]`);
        expect(resp).toMatchSnapshot();
        // @ts-ignore
        expect(resp.children[0].children[0].type).toEqual("wikiLink");
    });
    test("doesn't parse inline code block", () => {
        const resp = getProcessor().parse("`[[foo.md]]`");
        expect(resp).toMatchSnapshot("bond");
        // child1 paragraph, child2 link
        // @ts-ignore
        // expect(resp.children[0].children[0].data.link).not.toEqual({
        //   type: "file",
        //   name: "foo",
        //   anchorStart: undefined,
        //   anchorEnd: undefined,
        // });
    });
});
//# sourceMappingURL=dendronLinksPlugin.spec.js.map