import { ProtoLink, DEngine } from "@dendronhq/common-all";
import { Node } from "unist";
import { VFile } from "vfile";
declare type ReplaceRefOptions = {
    refReplacements?: {
        [key: string]: ProtoLink;
    };
    imageRefPrefix?: string;
    wikiLink2Md?: boolean;
    wikiLinkPrefix?: string;
    wikiLinkUseId?: boolean;
    engine?: DEngine;
};
export declare function replaceRefs(options: ReplaceRefOptions): (tree: Node, _file: VFile) => Node;
export {};
