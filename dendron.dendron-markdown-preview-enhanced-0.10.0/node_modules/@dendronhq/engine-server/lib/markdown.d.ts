import Token from "markdown-it/lib/token";
export declare type MDNode = {
    nodeType: "heading" | "other";
    openNode: Token;
    closeNode: Token;
    children: Token[];
};
export declare function md2MDNodes(txt: string): MDNode[];
