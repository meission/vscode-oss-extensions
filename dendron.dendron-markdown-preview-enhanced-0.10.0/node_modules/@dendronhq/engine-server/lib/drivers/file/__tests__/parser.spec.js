"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const common_server_1 = require("@dendronhq/common-server");
const parser_1 = require("../parser");
const lodash_1 = __importDefault(require("lodash"));
describe("schema parser ", () => {
    let root;
    beforeEach(async () => {
        root = await common_server_1.EngineTestUtils.setupStoreDir({
            storeDirSrc: "engine-server.parser",
        });
        console.log("done");
    });
    test("basic", () => {
        console.log(root);
        const schemas = parser_1.FileParserUtils.parseSchemaFile("foo.schema.yml", { root });
        expect(lodash_1.default.map(schemas, (s) => s.id).sort()).toEqual([
            "foo",
            "bar.bar",
            "bar.one",
            "baz.bar.bar",
            "baz.baz",
            "baz.bar.one",
            "baz.ns",
        ].sort());
        // @ts-ignore
        expect(lodash_1.default.find(schemas, { id: "foo" }).children).toEqual([
            "bar.bar",
            "baz.baz",
        ]);
        // @ts-ignore
        expect(lodash_1.default.find(schemas, { id: "bar.bar" }).children).toEqual(["bar.one"]);
    });
});
//# sourceMappingURL=parser.spec.js.map