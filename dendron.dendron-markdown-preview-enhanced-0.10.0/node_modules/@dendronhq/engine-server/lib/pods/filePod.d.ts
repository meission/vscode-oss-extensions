import { Note, NoteRawProps } from "@dendronhq/common-all";
import { Item } from "klaw";
import { BasePod, PodOpts } from "./base";
declare type DItem = Item & {
    data?: any;
    body?: string;
    entries: DItem[];
};
/**
 * Key is path, value is raw props
 */
declare type HierarichalDict = {
    [k: string]: NoteRawProps[];
};
declare type ImportOpts = {
    root: string;
};
export declare class FilePod extends BasePod<ImportOpts> {
    static id: string;
    static description: "Import from your local file system";
    static importOpts: PodOpts;
    fetch(): Promise<void>;
    /**
     * Return list of note props keyed by level of hierarchy
     * @param files
     */
    files2HierarichalDict(files: DItem[], root: string): HierarichalDict;
    hDict2Notes(hdict: HierarichalDict): Note[];
    handleImport(opts: ImportOpts): Promise<void>;
}
export {};
